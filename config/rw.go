package config

/* readConfig reads the configuration file. */
func (c *Config) ReadConfig() {

}

/* WriteConfig writes the config as a json. Called on program end to record updates in the config file. Pretty-prints the JSON with indentation. */
func (c *Config) WriteConfig() {

}
